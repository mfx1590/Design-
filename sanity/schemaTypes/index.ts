import type { SchemaTypeDefinition } from "sanity";

/**
 * Content model per PLAN.md section 10, populated in Phase 3:
 * siteSettings, package, service, project, review, furnitureItem, category, city, guide, faq, page.
 */
export const schemaTypes: SchemaTypeDefinition[] = [];
